sim_bev_preview:
  ros__parameters:
    image_topic: /camera/image_raw
    window_name: Metric IPM BEV
    window_scale: 2.0
    # Place to the right of D-Racer Camera (640 wide @ x≈40).
    window_x: 700
    window_y: 40
    show_guides: true
    # Empty → scripts/vision_tune DEFAULT_CONFIG_PATH (config/lane_vision.yaml)
    config_path: ''
